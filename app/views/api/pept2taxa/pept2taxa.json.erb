<%=
  Oj.dump(
    @input_order.map do |peptide|
      v = @result[peptide]
      v.map do |p|
        res = { "peptide" => peptide,
                "taxon_id" => p.id,
                "taxon_name" => p.name,
                "taxon_rank" => p.rank,
        }
        if @extra_info
          res.merge!(lineage_info(p.lineage, @names))
        else
          res
        end
      end
    end.flatten).html_safe
%>
