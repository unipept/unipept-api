[package]
name = "fixtures"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

# Deliberately empty: workspace deps here would cycle with the crates that dev-depend on this one.
[dependencies]
